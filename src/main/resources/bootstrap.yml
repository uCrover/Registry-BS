spring:
  application:
    name: registry-service
    #configuración del config-server
  cloud:
    config:
      uri: https://bs-config-server.azurewebsites.net
      username: nttdata
      password: nttd4t4

