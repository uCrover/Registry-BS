spring:
  application:
    name: registry-service
    #configuración del config-server
  cloud:
    config:
      uri: http://localhost:8090
      #uri: https://config-server-app.azurewebsites.net
      username: pr0j3ct
      password: pr0j3ct

